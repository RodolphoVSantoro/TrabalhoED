#include <stdio.h>
#include <stdlib.h>
#include "pizza.h"

int main(){
	ArvBM arvPizza = cria_arvore("pizzas", 3);
	Pizza *p1 = cria_pizza();
	arvPizza = cria_folha(arvPizza, );

	return 0;
}